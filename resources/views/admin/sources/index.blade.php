@extends('admin.layout')
@section('content')
<div class="nav"><div><h1 style="margin:0 0 6px">Website sources</h1><div class="muted">Only active origins can submit forms. Each origin owns its recipient.</div></div><a class="button secondary" href="{{ route('admin.submissions.index') }}">Submissions</a></div>
<div class="card" style="margin-bottom:20px"><h2 style="margin-top:0">Register website</h2><form method="POST" action="{{ route('admin.sources.store') }}" class="actions">@csrf<input type="url" name="origin" placeholder="https://platform-a.example" required><input type="email" name="recipient" placeholder="sales@example.com" required><button type="submit">Add source</button></form></div>
<div class="card"><table><thead><tr><th>Origin</th><th>Recipient</th><th>Status</th><th>Manage</th></tr></thead><tbody>@forelse($sources as $source)<tr><td>{{ $source->origin }}</td><td><form method="POST" action="{{ route('admin.sources.update',$source) }}" class="actions">@csrf @method('PATCH')<input type="email" name="recipient" value="{{ $source->recipient }}" required></td><td><select name="is_active"><option value="1" @selected($source->is_active)>Active</option><option value="0" @selected(! $source->is_active)>Disabled</option></select></td><td><button type="submit">Save</button></form></td></tr>@empty<tr><td colspan="4" class="muted">No website sources registered.</td></tr>@endforelse</tbody></table></div>
@endsection
