@extends('admin.layout')
@section('content')
<style>.edit-source{max-width:680px}.source-field{margin-bottom:18px}.source-field label{display:block;margin-bottom:7px;color:#526076;font-size:12px;font-weight:700}.source-field input,.source-field select{width:100%}.source-note{margin-top:8px;color:#718096;font-size:12px;line-height:1.55}</style>
<div class="page-head"><div><div class="eyebrow">Source configuration</div><h1>Edit website source</h1><div class="muted">Update the trusted origin and its message routing details.</div></div><a class="button secondary" href="{{ route('admin.sources.index') }}">Back to sources</a></div>
<section class="card card-pad edit-source"><form method="POST" action="{{ route('admin.sources.details.update', $source) }}">@csrf @method('PATCH')<div class="source-field"><label for="origin">Website origin</label><input id="origin" name="origin" type="url" value="{{ old('origin', $source->origin) }}" required><div class="source-note">Use the exact browser origin, for example <code>https://platform.example</code>. Changing it immediately changes which website may submit.</div></div><div class="source-field"><label for="recipient">Recipient inbox</label><input id="recipient" name="recipient" type="email" value="{{ old('recipient', $source->recipient) }}" required></div><div class="source-field"><label for="is_active">Submission access</label><select id="is_active" name="is_active"><option value="1" @selected(old('is_active', $source->is_active) == 1)>Active - accept form submissions</option><option value="0" @selected(old('is_active', $source->is_active) == 0)>Disabled - reject form submissions</option></select></div><div class="actions"><button class="button" type="submit">Save source</button><a class="button secondary" href="{{ route('admin.sources.index') }}">Cancel</a></div></form></section>
@endsection
